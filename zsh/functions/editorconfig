editorconfig() {
  cat > .editorconfig << 'EOF'
root = true
[*]
charset = utf-8
insert_final_newline = true
end_of_line = lf
indent_style = tab
EOF
  echo ".editorconfig file created in the current directory."
}
